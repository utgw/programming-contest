#include <bits/stdc++.h>
using namespace std;
#define FOR(i,m,n) for(int i=m;i<(n);i++)
#define REP(i,n) FOR(i,0,n)

int main(){
  int N,M,X,Y;
  cin >> N >> M >> X >> Y;
  vector<int> a(N), b(M);
  REP(i,N) cin >> a[i];
  REP(i,M) cin >> b[i];
  return 0;
}